# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

selfhost/.env.staging
selfhost/.env.*
!selfhost/.env.example

# Capacitor native shells — generated locally with
#   npx cap add android
#   npx cap add ios
# Both contain platform SDK output (Pods, .gradle, build/) that must not
# be committed; they are regenerated from `capacitor.config.ts` + dist/.
android/
ios/